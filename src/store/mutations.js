export default {
  SET_TOKEN (state, token) {
    state.token = token
  },
  SET_MY_INFO (state, playload) {
    state.my_info = playload
  },
  SET_NAV_CONFIG (state, playload) {
    state.nav_config = playload
  }
}
